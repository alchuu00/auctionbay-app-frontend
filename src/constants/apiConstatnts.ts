export enum apiRoutes {
  AUTH_PREFIX = "/auth",
  LOGIN_PREFIX = "/auth/login",
  SIGNUP_PREFIX = "/auth/register",
  SIGNOUT_PREFIX = "/auth/signout",
  REFRESH_TOKENS_PREFIX = "/auth/refresh",

  USERS_PREFIX = "/users",
  UPLOAD_AVATAR_IMAGE_PREFIX = "/users/upload",

  AUCTIONS_PREFIX = "/auctionItems",
  UPLOAD_AUCTION_IMAGE_PREFIX = "/auctionItems/upload",
  
  BIDS_PREFIX = "/bids",

  RESET_PASSWORD_PREFIX = "/reset-password",
  
  NOTIFICATIONS_PREFIX = "/notifications",
}
